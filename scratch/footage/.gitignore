# Source video is never committed; only the frames extracted into assets/seq/.
*.mp4
*.mov
*.webm

# ffmpeg concat manifest, regenerated per extraction and pointing at the
# ignored sources above.
concat.txt
